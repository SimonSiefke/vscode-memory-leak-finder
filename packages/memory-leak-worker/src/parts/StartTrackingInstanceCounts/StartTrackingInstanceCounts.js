export const startTrackingInstanceCounts = (session) => {
  // TODO
  // 1. find all instances
  // 2. modify constructor to point to custom class
  //    that captures stack trace on creation
  //    and calls the original prototype constructor
}
